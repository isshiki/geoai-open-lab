# Copy to .env and set a PERSONAL Places Portal access token locally.
# Never paste a token into a notebook, command line, chat, or this example.
# Places API key (not used by this Iceberg experiment).
FSQ_API_KEY=
FSQ_OS_PLACES_TOKEN=
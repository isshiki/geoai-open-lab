# Reserved for future local configuration. No credentials are needed for setup.
# Copy to .env only when an experiment requires it; .env is never committed.
# Do not put real tokens, passwords, cookies, or signed URLs in this example.
